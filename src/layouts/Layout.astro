---
// Styles
import "@/styles/utils.css";

import Header from "@/components/Header.astro";
import Footer from "@/components/Footer.astro";

import { PARTNER_ID, PROJECT_TYPE } from "astro:env/client";

interface Props {
  title: string;
  description?: string;
}

const { title, description } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="preconnect" href="https://p.typekit.net" crossorigin />
    <link rel="preconnect" href="https://use.typekit.net" crossorigin />
    <link rel="preload" as="style" href="https://use.typekit.net/fim8cbh.css" />
    <link
      rel="stylesheet"
      href="https://use.typekit.net/fim8cbh.css"
      media="print"
      onload="this.media='all'"
    />

    <link
      href=`https://admiss.info/assets/lp-live/${PARTNER_ID}/${PROJECT_TYPE}/styles.css`
      rel="stylesheet"
    />
  </head>
  <body>
    <div class="page-wrap">
      <Header />
      <slot />
    </div>
    <Footer />
    <script src="https://admiss.info/acquia-template/alr-plugins.js" defer
    ></script>
    <script src="https://admiss.info/acquia-template/alr-global.js" defer
    ></script>
    <script
      src="https://admiss.info/acquia-template/alr-landing-page-v2.0-migrated-min.js"
      defer></script>
  </body>
</html>
<style is:global>
  :root {
    --accent: 136, 58, 234;
    --accent-light: 224, 204, 250;
    --accent-dark: 49, 10, 101;
    --accent-gradient: linear-gradient(
      45deg,
      rgb(var(--accent)),
      rgb(var(--accent-light)) 30%,
      white 60%
    );
  }
  html {
    color: #231f20;
    font-family: lato, Arial, sans-serif;
    font-size: 1rem;
    line-height: 1.4;
    scroll-behavior: smooth;
  }
  code {
    font-family:
      Menlo,
      Monaco,
      Lucida Console,
      Liberation Mono,
      DejaVu Sans Mono,
      Bitstream Vera Sans Mono,
      Courier New,
      monospace;
  }
</style>

---
import { ACS_URL } from "astro:env/client";
interface Props {
  formId: string;
}

const { formId } = Astro.props;
---

<script is:inline src=`//${ACS_URL}/form/generate.js?id=${formId}`></script>
